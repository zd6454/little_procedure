<!--component/start/start.wxml-->
<view class='itembox'>
  <view wx:for="{{imgs}}" wx:key="id" bindtap='select' data-index="{{item.id}}">
    <image class="star" src="{{item.id>starId?src2:src1}}"></image>
  </view>
  <!-- <view class='scorePin'>{{starId}}.00</view> -->
</view>
